# JOS One

The operating system for The JAE Edit.

## Development

```bash
npm install
npm test
npm run dev
```

## Deployment

Push to `main`. GitHub Actions runs tests, builds the React app, and deploys `dist` to GitHub Pages.
